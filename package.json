{
  "name": "pingpong",
  "version": "1.0.0",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "author": "Zyabrik10",
  "license": "ISC",
  "description": "The ping pong to play with your friend",
  "dependencies": {
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
